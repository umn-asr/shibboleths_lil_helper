# TODO: Set better example (DEMAND THIS FROM JOE if you are reading this!!!!!!!!!!!)
Slh.for_strategy :todo_your_org_against_test_idp do
  for_host 'somehost.com' do
    set :shib_prefix, "/swadm/etc/shibboleth"
    for_site 'somesite.com' do
      protect 'somepath'
    end
    for_site 'somesite2.com' do
      protect 'secure'
      # Could also say 
      # protect 'lazy_auth', :flavor => :authentication_optional
      # OR
      #
      # Could also say 
      # protect 'users', :flavor => :authentication_required_for_specific_users
      # set :specific_users, %w(goggins@umn.edu ding0057@umn.edu)
    end
  end
end
