# Auto-generated <%= Time.now.to_s %> from Shibboleths Lil Helper.
# You should put this in the appropriate Apache vhost config file
# associated with
#   Site:     <%= @site.name %>
#   Host:     <%= @host.name %>
#   Strategy: <%= @strategy.name %>
#
<% if @site.paths.empty? %>
# WARNING: No paths specified to protect
<% else %>
  <% @site.paths.each do |path| %>
<%= path.to_apache_directive(:strategy => @strategy, :host => @host, :site => @site) %>
  <% end %>
<% end %>

