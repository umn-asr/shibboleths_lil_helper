# THIS SHOULD BE INCLUDED via an Apache Include statement or copy and
# pasted in your
# <VirtualHost AN_IP_ADDR:80> and <VirtualHost AN_IP_ADDR:443> sections
<% if @site.paths.empty? %>
  # YOU HAVE NOT SPECIFIED ANY PATHS TO BE PROTECTED
  # TIZ WEIRD DUDE, YOU SURE YOU WANT TO DO THIS?
<% else %>
  <% @site.paths.each do |path| %>
    <%= path.to_apache_directive(:strategy => @strategy, :host => @host, :site => @site) %>
  <% end %>
<% end %>

